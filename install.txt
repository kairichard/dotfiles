on osx:
	install brew
all:
	autojump
	ssh-keygen
	install vim
	install git
	install tmux > 1.7

	dotfiles:
		cd ~
		git clone git@github.com:kairichard/dotfiles.git
		cd dotfiles
		git submodule init;
		git submodule update
		rake install
		cd vim; ./install_bundles
on osx:
	cd resources; cp *.ttf FONTS_DIR
